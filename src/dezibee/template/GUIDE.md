# DeziBee 原型工作台使用说明（SKILL）

> 本文件给 DeziBee(AI) 阅读。你负责在 `WORKBENCH_DATA` 中创建页面、原型卡片、
> 交互关系和 PRD；框架（index.html / css / js）负责承载与联动，**不要修改框架代码**。

## 术语对照（先读这个，分清用户要改的对象）

工作台三栏的固定称呼，以及用户各种说法对应到你要改的数据：

| 用户说的 | 对应三栏 / 对象（你要改的数据） |
|---|---|
| 画布导航 / 左侧导航 / 导航 / 页面列表 | **左栏「画布导航」** = `pages[]` 页面树。导航树由 pages 自动渲染，**没有独立的导航数据**——改导航（增删/改名/分组/排序）就是改 `pages` |
| 画布 / 画布详情 / 中间画布 | **中栏「画布详情」** = 无限画布，展示原型页面与卡片。内容由 `pages` / `cards` 定义 |
| 原型页面 / 页面 | `pages[]` 中的一项（含它上面的 cards） |
| 原型卡片 / 卡片 | `pages[].cards[]` 中的卡片：改外观/内容是改 `card.html`，改尺寸是 `w/h`，改壳是 `shell`，改位置是 `x/y` |
| PRD / 需求说明 / 产品文档 / 产品需求说明书 / 右栏 | **右栏「产品需求说明书」** = `prd.sections[]` 章节。改章节标题/层级/内容就是改 `prd.sections` |
| 需求 | DeziBee 需求列表里的需求（本项目元信息，在 App 侧管理），与上面三栏无关 |

**收到修改请求时，先按此表确定唯一数据对象再动手**；两种对象都可能涉及（如"这个页面的说明"
= 卡片 + PRD 章节）时两边都改并保持 `prdId` 关联；仍无法确定改哪个时用 ask_user 澄清。

## 一、你在哪里工作

需求目录结构：

```
demo/
  index.html      ← 工作台入口（预览/部署的根）
  css/workbench.css
  js/workbench.js
  GUIDE.md        ← 本文件
prd/              ← 可选：更完整的独立 PRD 文档
uploads/          ← 用户上传的参考材料（只读）
```

**修改的唯一入口**：`demo/index.html` 里的 `const WORKBENCH_DATA = {...}`。
新建完整数据可用 read_file / write_file / write_file_chunk（超过 3000 字用分块写入）；修改已有数据时，先用 `read_file_range` 读取当前区域，再用 `insert_text` 的 1–3 行唯一纯文本锚点定位。**不要使用 `edit_file`**，也不要以数组结尾、整段 PRD 或 `\\u003c` 这类转义文本作锚点；锚点找不到时重新读取当前区域，不能盲目重试。

## 二、数据模型（唯一 ID 是核心）

```js
WORKBENCH_DATA = {
  project: { title: "项目名" },

  pages: [                       // 页面（第一项固定为需求概述，业务页面从第二项开始）
    {
      id: "page-<唯一短id>",      // 必须全局唯一
      name: "页面名",
      group: "主流程",           // 多级分组用 "一级/二级"；空串=顶级
      prdId: "sec-<章节id>",     // 可选：关联的页面级 PRD 章节（切到该页时右栏自动定位）
      cards: [ ... ]            // 该页面画布（中栏「画布详情」）上的原型卡片
    }
  ],

  links: [                       // 卡片间交互关系（画布连线）
    {
      id: "lnk-<唯一短id>",
      from: "card-xxx",          // 来源卡片 id
      to: "card-yyy",            // 目标卡片 id（可跨页面）
      trigger: "点击「提交」",     // 触发行为
      condition: "表单校验通过",   // 条件（可省略）
      note: "提交成功后跳转"       // 说明（可省略）
    }
  ],

  prd: {
    sections: [                  // PRD 章节（右栏「产品需求说明书」长文档；顺序即文档顺序）
      {
        id: "sec-<唯一短id>",     // 全局唯一
        title: "章节标题",
        level: 2,                // 2/3/4 → h2/h3/h4，目录按 level 缩进
        html: "<p>…</p>"         // 章节内容（HTML 字符串）
      }
    ]
  }
}
```

## 三、原型卡片（核心）

### 第一页：需求概述（固定）

- `pages[0]` 必须是「需求概述」页，必须放在所有具体业务页面之前。
- 需求概述页只展示本次原型的概要信息，不承载具体业务操作。
- 概述卡片通常展示：需求名称、版本信息、更新时间、需求状态、负责人等；没有确切信息时写「待补充」，不要编造。
- 具体业务页面从 `pages[1]` 开始创建；不要把首页、登录页等业务页面放在第一页取代需求概述页。
- 需求概述页对应的 PRD 章节放在 `prd.sections` 第一项，内容包括：本次需求背景和目标、需求概述；涉及跨页面流程时补充完整的文字流程说明，以及必要的其他概要信息。
- 后续 PRD 章节再按具体业务页面和原型卡片编写页面元素、交互和状态。

卡片 = 画布上的一个独立节点，字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| id | ✓ | 全局唯一，如 `card-login-default` |
| name | ✓ | 显示名 |
| type | 建议 | 自由定义：`page` / `dialog` / `state` / `flow` / `component`… |
| x, y | ✓ | 画布坐标（自由布局，注意留出间距 120+，给外壳留位） |
| w, h | ✓ | **屏幕内容区尺寸（CSS 像素 1:1）**，按设备实际尺寸定（见下表） |
| shell | 建议 | 系统外壳组件：`phone` 手机 / `tablet` 平板 / `browser` 浏览器；省略=无边框裸卡片 |
| status | 建议 | `draft` / `confirmed` / `deprecated`（可自由扩展） |
| prdId | ✓ | **关联的 PRD 章节 id（双向联动靠它，不要靠名称）** |
| html | ✓ | **真实 HTML/CSS/JS 原型。禁止用图片模拟页面** |

### 实际尺寸与外壳（重要，直接决定观感）

- **按设备真实 CSS 像素 1:1 生成**，不要缩小尺寸凑画布——画布可缩放平移，清晰度优先：
  - 手机竖屏页面：`w:375, h:812` + `shell:"phone"`（逻辑分辨率 iPhone 类）；
  - 平板竖屏页面：`w:820, h:1180` + `shell:"tablet"`；
  - 桌面/网页页面：`w:1440, h:900` + `shell:"browser"`（内容按 1440 设计）；
  - 弹窗/局部状态：跟随所属平台的尺寸（手机弹窗约 340×auto、桌面弹窗约 480×360），
    同平台统一用同一种 shell。
- `w/h` 始终指**屏幕内容区**；手机壳（圆角边框+刘海）、平板壳（窄边框）、
  浏览器壳（窗口顶栏+地址栏）由框架按 `shell` 自动绘制并适当扩展外围，
  你不需要也不应该用 HTML/CSS 画壳或画状态栏。
- 用户在 DeziBee 功能区选择的「设备外壳」是**默认壳**：新页面/页面级卡片默认用它；
  你仍可按卡片覆盖（如桌面 demo 里某张手机弹窗卡片用 `phone`）。
- shell 内容由你写满：手机页面内容按 375 宽设计、桌面页面按 1440 宽设计，
  **不要在 375 的壳里塞 1440 宽的桌面布局**（反之亦然）。
- **手机/平板安全区**：壳顶部有刘海/边框（手机约 54px、平板约 40px），
  页面第一个元素请加对应的 `padding-top`/`margin-top` 避开（如导航栏放其下），
  状态栏本身由外壳绘制，不要自己画时间/电量。
- **滚动条由框架处理**：原生滚动条已隐藏，页面滚动时右缘会浮现 3px 的浮动指示条
  （停止后自动消失）。不要自绘滚动条，也不要依赖滚动条宽度做布局。

`html` 写法要求：
- 只写 body 内片段（框架已包 iframe 文档）；样式可内联 `<style>`，脚本可用；
  不要写 `<html>/<body>` 外壳。
- 交互要真实可用：按钮能点、表单能填、弹窗能开（iframe 沙箱允许 scripts/forms/modals）。
- 页面级原型建议自带 `font-family:system-ui` 等基础样式；字体大小用真实设备
  逻辑像素（手机正文 14-16px、桌面正文 14px），勿因画布缩小而加大字号。

**什么值得成为独立卡片**（核心原则）：一个需要开发明确理解的独立视觉/交互状态。
例如：登录页默认态、登录页错误态、支付弹窗、下单成功页。

## 四、PRD 章节

- PRD 第一章固定对应第一页「需求概述」，用于说明本次迭代的背景、目标、需求概述和必要的全流程文字说明；保持简短，不写成完整项目文档。
- 从第二章开始，按具体业务页面或原型卡片说明页面元素、交互、状态和页面跳转；只覆盖本次迭代实际涉及的内容。
- 需求很小时，第一章和页面章节即可，不要强行添加接口、权限、埋点、性能等无关章节。
- 每个**卡片**必须有对应章节（`card.prdId` → `section.id`），在章节里表达：
  页面设计 / 结构 / 功能 / 元素 / 交互 / 页面跳转；按实际需要补充状态、异常或业务规则。
- 除第一页外，每个具体页面的 PRD 按以下顺序组织；不适用的项可以省略，用户也可以要求 AI 补充其他开发所需信息：
  1. 业务需求概述
  2. 页面结构
  3. 元素说明（按区域描述整个页面；每个区域包含展示组件、字段、功能按钮等信息）
  4. 业务规则（业务规则、交互、跳转、数据处理等）
  5. 数据/API
  6. 报错及异常
- **自动编号规则**：PRD 章节必须按层级连续编号，编号写入章节标题或正文小标题：一级 `1.`、二级 `1.1.`、三级 `1.1.1.`、四级 `1.1.1.1.`；新增内容继续使用当前层级的下一个编号，不得重置或跳号。页面级章节建议使用二级编号，页面下的上述内容使用三级编号。
- 编号只按 PRD 文档顺序和章节层级计算，不使用页面名称、卡片名称或 ID 代替编号；修改已有 PRD 时保留未修改章节的编号，新增章节插入后顺延后续编号。
- 页面也可加页面级章节（`page.prdId` → `section.id`）：用户切换到该页时右栏
  自动定位到它；页面未写 `prdId` 时自动退回该页第一张带 `prdId` 的卡片章节。
- 无卡片的页面级说明也可以加纯章节（不需要 prdId 对应）。
- 章节 `html` 中引用某张卡片时，用原型引用标签（点击 → 画布聚焦该卡片）：

```html
参见 <span class="prd-ref" data-card-ref="card-login-default">登录页原型</span>
```

- 也可以用 `<a href="#sec-章节id">` 做章节内跳转（框架给每个章节容器加了
  `id="sec-<章节id>"` 前缀，注意 `#sec-` 前缀要带上）。

### 用户可直接手改 PRD（重要）

通过 DeziBee「预览」在浏览器打开时，右栏 PRD 有「✎ 编辑」按钮：用户可以不经过你，
直接改章节标题 / 层级 / 正文并「保存」——保存会**写回同一份 `demo/index.html`** 的
`WORKBENCH_DATA.prd`（只替换 prd 字段，pages/links 不受影响）。

对你的要求：
- 每次改 PRD 前**先 read_file 重新读取** `demo/index.html`，因为用户可能刚手改过，
  不要基于记忆里的旧内容覆盖。
- 合并时保留用户的手改；确实需要推翻时，在回复里说明改了哪几条，别默默覆盖。
- 静态部署（OSS / GitHub Pages）或 `file://` 打开时没有保存端点，编辑入口自动隐藏。

## 五、双向关联（必须遵守）

- 页面 → PRD：左栏**切换页面** → 右栏 PRD 自动滚动到该页对应章节并高亮
  （映射：`page.prdId` 优先，否则该页第一张带 `prdId` 的卡片）。
- 卡片 → PRD：浏览器里**单击选中卡片** → PRD 自动滚动到对应章节并高亮。
- PRD → 卡片：点击章节里的 `data-card-ref` 引用 → 画布切到所属页面并聚焦高亮卡片。
- 关联全靠 `id`（`card.prdId` / `data-card-ref`）。**禁止**依赖名称匹配。

## 六、画布操作（给用户看的提示）

- 拖空白处平移；滚轮缩放；双击空白=适应全部内容
- `0` 回原点；`Ctrl+1` 适应内容；`Esc` 取消选中
- 卡片头部可拖动移动位置；工具栏「◎ 聚焦」聚焦选中卡片
- **PRD 宽度**：拖动中栏与右栏之间的竖直拖拽条可调整宽窄（双击复位）；宽度会本地记忆
- **PRD 编辑区高度**：编辑态下拖动正文框下方的小横条可加高/收窄（高度会本地记忆）
  —— 这两项纯属展示偏好，不改数据，AI 无需处理。

## 七、页面管理（AI 对话驱动）

用户新增/删除/重命名/排序页面时，直接修改 `pages` 数组：
- 新增：`pages.push({...})` 的数据形式（即编辑 index.html 中的数组文本）
- 删除：从数组移除；**同时**清理其卡片的 links 与对应 PRD 章节，避免死链
- 重命名：改 `name`（id 不变，关联不断）
- 排序：调整数组顺序（导航树按数组顺序渲染；同组内按顺序、组间按组名字典序）

## 八、检查清单（每轮修改后自查）

1. `pages[0]` 是需求概述页，具体业务页面从 `pages[1]` 开始
2. 需求概述页包含需求名称、版本信息、时间等概要信息；未知信息标记为「待补充」
3. PRD 第一章包含本次需求背景和目标、需求概述；涉及流程时补充完整文字流程
4. 所有 `pages[].id` / `cards[].id` / `sections[].id` / `links[].id` 全局唯一
5. 每张卡片有 `prdId`，且能在 `prd.sections` 里找到同名 `id`
6. `links` 的 from/to 都指向存在的卡片；删除卡片时同步清理
7. `html` 里没有绝对路径引用（部署到 OSS 后仍可用）；引用图片放 `demo/assets/` 并用相对路径
8. 修改后建议用户点「预览」在浏览器验收

## 九、导出与部署（单文件 HTML）

`demo/` 目录本身（index.html + css/ + js/）可直接整体复制到静态托管。
如果托管方**只允许上传一个文件**，用「导出单文件」：

- DeziBee 顶栏 **⤓ 导出** 按钮 → 在 `demo/` 下生成 `<需求ID>.html`（自包含，可直接上传）；
- 或在「预览」页左上角点 **⤓ 导出** 下载同名文件。

打包逻辑（`dezibee/core/packager.py`）：把 `css/` `js/` 内联进 HTML，本地图片/字体转 data URI，
产物**零外部请求**，并注入 `window.__DEZIBEE_EXPORT__=true` 隐藏「编辑 PRD / 导出」等依赖本机
服务器的入口。所以：

- **不要**在 `html` 里写绝对路径或依赖运行时网络请求的脚本，否则导出后可能失效；
- 图片走 `demo/assets/` 相对路径即可，导出时会自动内联；
- 外部 CDN 引用（如在线字体、远程图片）**不会**被内联，导出后仍需能访问该外链。
